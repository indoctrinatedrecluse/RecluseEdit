<UserControl x:Class="RecluseEdit.Extensions.Database.UI.DatabasePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="360"
             Background="#1E1E1E" Foreground="#D4D4D4">

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Top Bar -->
            <RowDefinition Height="Auto" /> <!-- Tables Expander -->
            <RowDefinition Height="120" MinHeight="60" /> <!-- Query Box -->
            <RowDefinition Height="Auto" /> <!-- Run Button & Splitter -->
            <RowDefinition Height="*" MinHeight="100" /> <!-- Results DataGrid -->
            <RowDefinition Height="Auto" /> <!-- Status Footer -->
        </Grid.RowDefinitions>

        <!-- 0. Top Bar: Open SQLite DB -->
        <Border Grid.Row="0" Background="#252526" Padding="6,4" CornerRadius="3" Margin="0,0,0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button x:Name="BtnOpenDb" Content="📁 Open SQLite DB" Padding="8,4" Background="#007ACC" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" Click="OnOpenDbClick" />
                <TextBlock x:Name="TxtDbName" Grid.Column="1" Text="No database open" Foreground="#808080" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0" TextTrimming="CharacterEllipsis" />
            </Grid>
        </Border>

        <!-- 1. Tables & Schema Tree -->
        <Expander x:Name="ExpanderTables" Grid.Row="1" Header="TABLES &amp; SCHEMA" IsExpanded="False" Margin="0,0,0,6" Foreground="#9CDCFE" FontWeight="SemiBold" FontSize="11">
            <Border Background="#252526" CornerRadius="3" Padding="4" MaxHeight="140">
                <TreeView x:Name="TreeTables" Background="Transparent" BorderThickness="0" Foreground="#D4D4D4" MouseDoubleClick="OnTreeItemDoubleClick">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Columns}">
                            <StackPanel Orientation="Horizontal" Margin="0,1">
                                <TextBlock Text="🗄️ " FontSize="11" />
                                <TextBlock Text="{Binding Name}" FontWeight="Medium" Foreground="#DCDCAA" />
                            </StackPanel>
                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="4,1">
                                        <TextBlock Text="• " Foreground="#808080" />
                                        <TextBlock Text="{Binding Name}" Foreground="#9CDCFE" />
                                        <TextBlock Text=" (" Foreground="#808080" />
                                        <TextBlock Text="{Binding Type}" Foreground="#4EC9B0" FontSize="11" />
                                        <TextBlock Text=")" Foreground="#808080" />
                                    </StackPanel>
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>
        </Expander>

        <!-- 2. SQL Query Editor Box -->
        <TextBox x:Name="TxtQuery" Grid.Row="2"
                 Background="#252526" Foreground="#D4D4D4" CaretBrush="#D4D4D4"
                 BorderBrush="#3F3F46" BorderThickness="1"
                 AcceptsReturn="True" AcceptsTab="True"
                 FontFamily="Consolas, Courier New" FontSize="12"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Padding="6" KeyDown="OnQueryKeyDown" />

        <!-- 3. Query Actions Bar -->
        <Grid Grid.Row="3" Margin="0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnRunQuery" Content="▶ Run Query (Ctrl+Enter)" Background="#007ACC" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Padding="10,4" Cursor="Hand" Click="OnRunQueryClick" />
            <Button x:Name="BtnExportCsv" Grid.Column="2" Content="Export CSV" Background="#2D2D2D" Foreground="#D4D4D4" BorderBrush="#3F3F46" BorderThickness="1" Padding="8,4" Cursor="Hand" Click="OnExportCsvClick" />
        </Grid>

        <!-- 4. Results DataGrid -->
        <DataGrid x:Name="GridResults" Grid.Row="4"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Background="#252526"
                  Foreground="#D4D4D4"
                  RowBackground="#252526"
                  AlternatingRowBackground="#2D2D2D"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="#3F3F46"
                  VerticalGridLinesBrush="#3F3F46"
                  BorderBrush="#3F3F46"
                  BorderThickness="1"
                  HeadersVisibility="Column"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto" />

        <!-- 5. Status Footer -->
        <Border Grid.Row="5" Background="#252526" Padding="6,3" Margin="0,4,0,0" CornerRadius="2">
            <TextBlock x:Name="TxtStatus" Text="Ready. Open a database to query." FontSize="11" Foreground="#808080" />
        </Border>
    </Grid>
</UserControl>

