<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <RootNamespace>RecluseEdit.Extensions.Remote</RootNamespace>
    <AssemblyName>RecluseEdit.Extensions.Remote</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="AvalonEdit" Version="6.3.1.120" />
    <PackageReference Include="SSH.NET" Version="2026.0.0" />
    <ProjectReference Include="..\..\RecluseEdit.Sdk\RecluseEdit.Sdk.csproj" />
  </ItemGroup>

  <Target Name="CopyExtensionToHost" AfterTargets="Build">
    <PropertyGroup>
      <ExtensionOutputDir Condition="'$(SolutionDir)' != '' and '$(SolutionDir)' != '*Undefined*'">$(SolutionDir)bin\$(Configuration)\net10.0-windows\Extensions\Remote\</ExtensionOutputDir>
      <ExtensionOutputDir Condition="'$(ExtensionOutputDir)' == ''">$(MSBuildThisFileDirectory)..\..\bin\$(Configuration)\net10.0-windows\Extensions\Remote\</ExtensionOutputDir>
    </PropertyGroup>
    <ItemGroup>
      <ExtensionFiles Include="$(TargetDir)$(TargetName).dll" />
      <ExtensionFiles Include="$(TargetDir)$(TargetName).pdb" Condition="Exists('$(TargetDir)$(TargetName).pdb')" />
      <ExtensionFiles Include="$(TargetDir)Renci.SshNet.dll" Condition="Exists('$(TargetDir)Renci.SshNet.dll')" />
    </ItemGroup>
    <MakeDir Directories="$(ExtensionOutputDir)" />
    <Copy SourceFiles="@(ExtensionFiles)" DestinationFolder="$(ExtensionOutputDir)" SkipUnchangedFiles="true" />
  </Target>

</Project>
