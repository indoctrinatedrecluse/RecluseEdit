<UserControl x:Class="RecluseEdit.Extensions.DeepSeek.Views.DeepSeekChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="360">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PanelBg" Color="#1E1E1E" />
        <SolidColorBrush x:Key="HeaderBg" Color="#252526" />
        <SolidColorBrush x:Key="SectionBg" Color="#2D2D30" />
        <SolidColorBrush x:Key="UserBubbleBg" Color="#0E639C" />
        <SolidColorBrush x:Key="AssistantBubbleBg" Color="#252526" />
        <SolidColorBrush x:Key="ToolBubbleBg" Color="#1E2A38" />
        <SolidColorBrush x:Key="BorderBrushColor" Color="#3E3E42" />
        <SolidColorBrush x:Key="TextPrimary" Color="#D4D4D4" />
        <SolidColorBrush x:Key="TextSecondary" Color="#9DA5B4" />
        <SolidColorBrush x:Key="AccentColor" Color="#007ACC" />
        <SolidColorBrush x:Key="ButtonHover" Color="#3E3E42" />

        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonHover}" />
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PanelBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- 0. Sub-Header / Controls -->
            <RowDefinition Height="Auto" /> <!-- 1. Settings Drawer (Collapsible) -->
            <RowDefinition Height="*" />    <!-- 2. Message History -->
            <RowDefinition Height="Auto" /> <!-- 3. Status Bar -->
            <RowDefinition Height="Auto" /> <!-- 4. Prompt Input Area -->
        </Grid.RowDefinitions>

        <!-- 0. Sub-Header / Controls -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBg}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="0,0,0,1" Padding="8,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtModelBadge" Text="deepseek-chat" Foreground="{StaticResource AccentColor}" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center" />
                    <TextBlock Text=" • " Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center" />
                    <TextBlock x:Name="TxtActiveFileBadge" Text="Active File" ToolTip="Include active file context" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center" />
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" ToolTip="Clear Conversation" Click="OnClearChatClick">
                    <TextBlock Text="🗑️ Clear" FontSize="11" />
                </Button>

                <Button Grid.Column="2" Style="{StaticResource IconButtonStyle}" ToolTip="API &amp; Endpoint Settings" Click="OnToggleSettingsClick">
                    <TextBlock Text="⚙️ Config" FontSize="11" />
                </Button>
            </Grid>
        </Border>

        <!-- 1. Settings Drawer (Collapsible) -->
        <Border x:Name="SettingsDrawer" Grid.Row="1" Background="{StaticResource SectionBg}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="0,0,0,1" Padding="10" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="API Endpoint URL:" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,3" />
                <TextBox x:Name="TxtApiEndpoint" Background="#1E1E1E" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderBrushColor}" Padding="4,3" FontSize="11" Margin="0,0,0,8" />

                <TextBlock Text="API Key / Secret:" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,3" />
                <Grid Margin="0,0,0,8">
                    <PasswordBox x:Name="TxtApiKey" Background="#1E1E1E" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderBrushColor}" Padding="4,3" FontSize="11" />
                    <TextBox x:Name="TxtApiKeyVisible" Background="#1E1E1E" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderBrushColor}" Padding="4,3" FontSize="11" Visibility="Collapsed" />
                </Grid>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <CheckBox x:Name="ChkShowKey" Content="Show Key" Foreground="{StaticResource TextSecondary}" FontSize="10" Checked="OnToggleShowKey" Unchecked="OnToggleShowKey" VerticalAlignment="Center" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="Model: " Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center" />
                        <TextBox x:Name="TxtModelName" Width="110" Background="#1E1E1E" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource BorderBrushColor}" Padding="3,2" FontSize="11" />
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                    <Button Content="Save &amp; Close" Background="{StaticResource AccentColor}" Foreground="White" BorderThickness="0" Padding="10,4" FontSize="11" FontWeight="SemiBold" Cursor="Hand" Click="OnSaveSettingsClick" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 2. Message History -->
        <ScrollViewer x:Name="ChatScrollViewer" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="8,8">
            <StackPanel x:Name="MessagePanel">
                <!-- Welcome placeholder if empty -->
                <Border x:Name="WelcomeBorder" Background="{StaticResource SectionBg}" CornerRadius="6" Padding="14" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="👋 DeepSeek AI Assistant" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontSize="13" Margin="0,0,0,6" />
                        <TextBlock Text="Ask questions, request code edits, or ask to run terminal tasks. This AI can:" TextWrapping="Wrap" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,6" />
                        <TextBlock Text="• Read &amp; understand workspace files" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="8,0,0,2" />
                        <TextBlock Text="• Write and create project files directly" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="8,0,0,2" />
                        <TextBlock Text="• Execute terminal commands (with your approval)" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="8,0,0,8" />
                        <TextBlock Text="Click ⚙️ Config above to enter your DeepSeek API key." Foreground="{StaticResource AccentColor}" FontSize="11" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 3. Status Bar (Busy / Thinking indicator) -->
        <Border x:Name="StatusBarBorder" Grid.Row="3" Background="{StaticResource HeaderBg}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="0,1,0,0" Padding="8,4" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtStatus" Text="DeepSeek is generating..." Foreground="{StaticResource AccentColor}" FontSize="11" VerticalAlignment="Center" />
                <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" ToolTip="Cancel Generation" Click="OnStopClick">
                    <TextBlock Text="⏹ Stop" Foreground="#E06C75" FontSize="11" FontWeight="SemiBold" />
                </Button>
            </Grid>
        </Border>

        <!-- 4. Prompt Input Area -->
        <Border Grid.Row="4" Background="{StaticResource HeaderBg}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="0,1,0,0" Padding="8,6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBox x:Name="TxtInput" 
                         Grid.Row="0"
                         Background="#1E1E1E" 
                         Foreground="{StaticResource TextPrimary}" 
                         BorderBrush="{StaticResource BorderBrushColor}" 
                         Padding="6,6" 
                         FontSize="12" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap" 
                         MaxHeight="120" 
                         MinHeight="44" 
                         PreviewKeyDown="OnInputPreviewKeyDown" />

                <Grid Grid.Row="1" Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CheckBox x:Name="ChkIncludeActiveFile" Content="Attach current file" Foreground="{StaticResource TextSecondary}" FontSize="11" IsChecked="True" VerticalAlignment="Center" />

                    <Button x:Name="BtnSend" Grid.Column="1" Background="{StaticResource AccentColor}" Foreground="White" BorderThickness="0" Padding="12,5" FontWeight="SemiBold" FontSize="11" Cursor="Hand" Click="OnSendClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Send ➤" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
