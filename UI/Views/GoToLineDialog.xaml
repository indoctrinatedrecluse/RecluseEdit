<Window x:Class="RecluseEdit.UI.Views.GoToLineDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Go to Line - RecluseEdit"
        Icon="pack://application:,,,/Assets/app.ico"
        Height="190" Width="380"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgPrimary}"
        Foreground="{StaticResource FgPrimary}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtPrompt" 
                   Grid.Row="0"
                   Text="Type a line number between 1 and 100:"
                   FontSize="12" 
                   Foreground="{StaticResource FgPrimary}"
                   Margin="0,0,0,8" />

        <TextBox x:Name="TxtLineInput"
                 Grid.Row="1"
                 Background="{StaticResource BgSecondary}"
                 Foreground="{StaticResource FgPrimary}"
                 CaretBrush="{StaticResource AccentBlue}"
                 BorderBrush="{StaticResource BorderDark}"
                 BorderThickness="1"
                 FontSize="14"
                 Padding="8,5"
                 KeyDown="OnInputKeyDown" />

        <TextBlock Grid.Row="2"
                   Text="Tip: You can specify line:column (e.g. 42:15)"
                   FontSize="11"
                   Foreground="{StaticResource FgSecondary}"
                   Margin="0,6,0,0" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Go" Width="75" Height="26" Style="{StaticResource ToolbarButtonStyle}" Click="OnOkClick" IsDefault="True" Margin="0,0,8,0" />
            <Button Content="Cancel" Width="75" Height="26" Style="{StaticResource ToolbarButtonStyle}" Click="OnCancelClick" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
