<UserControl x:Class="RecluseEdit.UI.Controls.SourceControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="260"
             Background="{StaticResource BgSecondary}">

    <UserControl.Resources>
        <Style x:Key="GitActionIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource FgSecondary}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#33FFFFFF" />
                                <Setter Property="Foreground" Value="{StaticResource FgPrimary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BgTertiary}" Padding="8,6" BorderBrush="{StaticResource BorderDark}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="SOURCE CONTROL" FontWeight="SemiBold" FontSize="11" Foreground="{StaticResource FgSecondary}" />
                    <Border Background="#252526" CornerRadius="3" Padding="4,1" Margin="6,0,0,0">
                        <TextBlock x:Name="TxtBranch" Text="🌿 master" FontSize="11" Foreground="{StaticResource AccentBlue}" FontWeight="Medium" />
                    </Border>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource GitActionIconButtonStyle}" ToolTip="Pull from Remote" Click="OnPullClick">
                    <TextBlock Text="⬇️" FontSize="11" />
                </Button>
                <Button Grid.Column="2" Style="{StaticResource GitActionIconButtonStyle}" ToolTip="Push to Remote" Click="OnPushClick">
                    <TextBlock Text="⬆️" FontSize="11" />
                </Button>
                <Button Grid.Column="3" Style="{StaticResource GitActionIconButtonStyle}" ToolTip="Refresh Status" Click="OnRefreshClick">
                    <TextBlock Text="🔄" FontSize="11" />
                </Button>
            </Grid>
        </Border>

        <!-- Commit Input Area -->
        <Border Grid.Row="1" Padding="8,6" BorderBrush="{StaticResource BorderDark}" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBox x:Name="TxtCommitMessage" 
                         Height="54" 
                         AcceptsReturn="True" 
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Background="#1E1E1E" 
                         Foreground="{StaticResource FgPrimary}" 
                         CaretBrush="{StaticResource FgPrimary}"
                         BorderBrush="{StaticResource BorderDark}" 
                         Padding="6,4" 
                         FontSize="12"
                         KeyDown="OnCommitMessageKeyDown" />
                
                <Button x:Name="BtnCommit" 
                        Content="✓ Commit" 
                        Background="{StaticResource AccentBlue}" 
                        Foreground="White" 
                        FontWeight="SemiBold"
                        BorderThickness="0" 
                        Padding="8,5" 
                        Margin="0,6,0,0" 
                        Cursor="Hand"
                        Click="OnCommitClick" />
            </StackPanel>
        </Border>

        <!-- Changes List View -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,4">
                <!-- Staged Changes Section -->
                <Expander x:Name="ExpanderStaged" IsExpanded="True" Margin="0,0,0,4">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="STAGED CHANGES" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource FgSecondary}" />
                            <Border Background="#33FFFFFF" CornerRadius="8" Padding="5,1" Margin="6,0,0,0">
                                <TextBlock x:Name="TxtStagedCount" Text="0" FontSize="10" Foreground="{StaticResource FgPrimary}" />
                            </Border>
                        </StackPanel>
                    </Expander.Header>
                    <ItemsControl x:Name="ListStaged">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="6,3" Margin="2,1" CornerRadius="2" Cursor="Hand" MouseLeftButtonDown="OnFileItemClick">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1AFFFFFF" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FileName}" Foreground="{StaticResource FgPrimary}" FontSize="12" />
                                            <TextBlock Text="{Binding DirectoryPath}" Foreground="#808080" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center" />
                                        </StackPanel>

                                        <Border Grid.Column="1" Background="#252526" CornerRadius="2" Padding="4,1" Margin="4,0">
                                            <TextBlock Text="{Binding StatusBadge}" Foreground="{Binding BadgeColor}" FontSize="11" FontWeight="Bold" />
                                        </Border>

                                        <Button Grid.Column="2" Style="{StaticResource GitActionIconButtonStyle}" Content="➖" ToolTip="Unstage Changes" Click="OnUnstageFileClick" Tag="{Binding}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>

                <!-- Working Tree Changes Section -->
                <Expander x:Name="ExpanderChanges" IsExpanded="True">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="CHANGES" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource FgSecondary}" />
                            <Border Background="#33FFFFFF" CornerRadius="8" Padding="5,1" Margin="6,0,0,0">
                                <TextBlock x:Name="TxtChangesCount" Text="0" FontSize="10" Foreground="{StaticResource FgPrimary}" />
                            </Border>
                        </StackPanel>
                    </Expander.Header>
                    <ItemsControl x:Name="ListChanges">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="6,3" Margin="2,1" CornerRadius="2" Cursor="Hand" MouseLeftButtonDown="OnFileItemClick">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1AFFFFFF" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FileName}" Foreground="{StaticResource FgPrimary}" FontSize="12" />
                                            <TextBlock Text="{Binding DirectoryPath}" Foreground="#808080" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center" />
                                        </StackPanel>

                                        <Border Grid.Column="1" Background="#252526" CornerRadius="2" Padding="4,1" Margin="4,0">
                                            <TextBlock Text="{Binding StatusBadge}" Foreground="{Binding BadgeColor}" FontSize="11" FontWeight="Bold" />
                                        </Border>

                                        <Button Grid.Column="2" Style="{StaticResource GitActionIconButtonStyle}" Content="➕" ToolTip="Stage File" Click="OnStageFileClick" Tag="{Binding}" />
                                        <Button Grid.Column="3" Style="{StaticResource GitActionIconButtonStyle}" Content="↺" ToolTip="Discard Changes" Click="OnDiscardFileClick" Tag="{Binding}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>

                <!-- No Repo Message -->
                <TextBlock x:Name="TxtNoRepo" 
                           Text="No Git repository found in workspace." 
                           Foreground="#808080" 
                           FontSize="12" 
                           HorizontalAlignment="Center" 
                           Margin="0,40,0,0" 
                           Visibility="Collapsed" />
            </StackPanel>
        </ScrollViewer>

        <!-- Status / Feedback Toast Footer -->
        <Border Grid.Row="3" Background="{StaticResource BgTertiary}" Padding="8,4" BorderBrush="{StaticResource BorderDark}" BorderThickness="0,1,0,0">
            <TextBlock x:Name="TxtGitStatus" Text="Ready" FontSize="11" Foreground="{StaticResource FgSecondary}" TextTrimming="CharacterEllipsis" />
        </Border>
    </Grid>
</UserControl>

